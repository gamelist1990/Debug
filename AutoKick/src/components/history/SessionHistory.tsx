import type { ClientLog } from "../../client/types";
export function SessionHistory({ logs }: { logs: ClientLog[] }) { return <section className="history-page"><div className="section-heading"><div><p className="eyebrow">SESSION HISTORY / LOG</p><h2>セッション履歴ログ</h2><p>実行したセッションのログを確認できます。</p></div></div><div className="history-card">{logs.length === 0 ? <div className="empty-state">まだセッション履歴はありません。</div> : logs.map((log, index) => <div className="history-row" key={`${log.timestamp}-${index}`}><span className={`history-level ${log.level}`} /> <time>{new Date(log.timestamp).toLocaleString("ja-JP")}</time><p>{log.message}</p></div>)}</div></section>; }