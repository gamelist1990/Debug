import type { AccountProfile } from "../../client/types";

interface Props { accounts: AccountProfile[]; selected: string[]; onToggle: (id: string) => void; }
export function AccountSelector({ accounts, selected, onToggle }: Props) { return <section className="panel accounts"><div className="panel-title"><div><p className="eyebrow">01 / ACCOUNTS</p><h3>使用するアカウント</h3></div><button className="text-btn">＋ 追加</button></div><div className="account-list">{accounts.map((account) => <label className={`account ${selected.includes(account.id) ? "selected" : ""}`} key={account.id}><input type="checkbox" checked={selected.includes(account.id)} onChange={() => onToggle(account.id)} /><span className="check">✓</span><span className="account-avatar">{account.avatarUrl ? <img src={account.avatarUrl} alt="" /> : account.gamertag.slice(0, 2).toUpperCase()}</span><span className="account-info"><b>{account.gamertag}</b><small>XUID {account.xuid ?? "未取得"}</small></span><i className={`status ${account.status}`} /></label>)}</div><button className="manage">アカウント管理を開く <span>→</span></button></section>; }
